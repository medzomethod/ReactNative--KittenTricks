import React from "react";
import { Text, StyleSheet } from "react-native";

const PlaylistScreen = () => {
  return <Text style={styles.text}>PlaylistScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default PlaylistScreen;
