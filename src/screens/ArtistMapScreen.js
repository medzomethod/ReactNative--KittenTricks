import React from "react";
import { Text, StyleSheet } from "react-native";

const ArtistMapScreen = () => {
  return <Text style={styles.text}>ArtistMapScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default ArtistMapScreen;
