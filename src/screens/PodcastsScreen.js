import React from "react";
import { Text, StyleSheet } from "react-native";

const PodcastsScreen = () => {
  return <Text style={styles.text}>PodcastsScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default PodcastsScreen;
